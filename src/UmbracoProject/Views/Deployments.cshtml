@using Umbraco.Cms.Web.Common.PublishedModels;
@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage
@{
	Layout = "basicNav.cshtml";
}

<h1>Triggered API endpoint with the cloud web hook.</h1>

@Model.Value("deploymentData", fallback: Fallback.ToDefaultValue, defaultValue: (object)"No deployment data available")